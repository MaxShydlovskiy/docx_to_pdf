<% if !@conversion.errors.empty? %>
   <div class = "alert alert-error">
      
      <ul>
         <% @conversion.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
         <% end %>
      </ul>
      
   </div>
<% end %>

<div class = "well">
   <%= form_for @conversion, html: { multipart: true } do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>
      <%= f.label :attachments %>
      <%= f.file_field :attachments, multiple: true %>
      <%= f.submit "Save", class: "btn btn-primary" %>
   <% end %>
</div>
